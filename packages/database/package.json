{
  "name": "@zk-census/database",
  "version": "1.0.0",
  "description": "Database models and migrations for zk-Census",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "migrate": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback",
    "migrate:make": "knex migrate:make",
    "seed": "knex seed:run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zk-census/types": "workspace:*",
    "knex": "^3.1.0",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "typescript": "^5.3.3"
  }
}
