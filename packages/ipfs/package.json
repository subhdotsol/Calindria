{
  "name": "@zk-census/ipfs",
  "version": "1.0.0",
  "description": "IPFS integration for zk-Census",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@zk-census/types": "workspace:*",
    "ipfs-http-client": "^56.0.3",
    "it-all": "^3.0.4",
    "uint8arrays": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
